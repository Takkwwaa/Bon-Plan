{
  /* <ul className="list-none flex justify-end items-start flex-1 flex-col">
  <li className={"font-poppins font-normal cursor-pointer text-[16px] text-black mr-4"}>
    <a href={"#contact"}>Contact Us</a>
  </li>
  <li className={"font-poppins font-normal cursor-pointer text-[16px] text-black mr-4"}>
    <button
      onClick={() => {
        handleConnectClick("Connect As");
      }}
    >
      Login
    </button>
    {showPopup && <ConnectPopup onClose={handleClosePopup} title={title} />}
  </li>
  <li className={"font-poppins font-normal cursor-pointer text-[16px] text-black  mr-0"}>
    <button
      onClick={() => {
        handleConnectClick("Create a New");
      }}
    >
      Sign up
    </button>
    {showPopup && <ConnectPopup onClose={handleClosePopup} title={title} />}
  </li>
</ul>; */
}
